📊 DataLoadApp — GUI-приложение для анализа данных

Простой и мощный инструмент для загрузки, очистки, анализа и отчетности по данным.
Работает через графический интерфейс

Быстрый старт
1. Установка зависимостей
pip install -r requirements.txt

2. Запуск приложения
python main.py

---

## 🧰 Функции приложения

✅ 1. Загрузка данных
| Источник | Поддерживается |
|--------|--------------|
| CSV файл | ✅ |
| Excel файл | ✅ |
| PostgreSQL | ✅ |
| REST API (JSON) | ✅ |

#### Примеры:
- **CSV**: `data/customers.csv`
- **Excel**: `report.xlsx`, лист `Sales`
- **PostgreSQL**:
  Хост: `localhost`
  Порт: `5432`
  База: `analytics`
  SQL: `SELECT * FROM sales_data LIMIT 1000;`
- **REST API**:
  URL: `https://api.example.com/data`
  Заголовки: `Authorization: Bearer <token>`

---

✅ 2. Очистка данных (`data_cleaner.py`)
Обрабатывает пропущенные значения, кодирует категориальные переменные, масштабирует числовые.

#### Методы:

Обработка и замена NaN(средняя, медиана, удаление)
cleaned_df = cleaner.handle_missing_values(df, strategy="mean") # "median", "drop"

Кодирование категорий(onehot, label)
encoded_df = cleaner.encode_categorical(df, method="onehot") # "label"

Нормализация(Стандартная, minmax)
scaled_df = cleaner.scale_numeric(df, method="standard") # "minmax"

✅ 3. Анализ данных (`data_analyze.py`)
Выполняет статистический анализ, поиск аномалий и обучение моделей.

#### Методы:

Базовые статистики:
analyzer.basic_statistics()

Поиск выбросов (Z > 3):
analyzer.find_anomalies(z_thresh=3.0)

Регрессия(линейная или случайный лес):
analyzer.build_regression_model("target_col", model_type="linear") # "random_forest"

Классификация(логистическая или случайный лес):
analyzer.build_classification_model("target_col", model_type="logistic") # "random_forest"

### ✅ 4. Генерация отчётов (`data_report.py`)
Создаёт полный отчёт в PDF/Excel, с графиками и отправкой по email.

#### Методы:

Полная генерация
artifacts = report.build_full_report( out_dir="reports", make_pdf=True, make_excel=True, make_static_plots=True, make_interactive_plots=True )

Отправка по email:
DataReport.send_full_report_smtp(
artifacts=artifacts,
smtp_host="smtp.gmail.com",
smtp_port=587,
username="you@gmail.com",
password="app_password",
sender="you@gmail.com",
recipients=["team@example.com"],
subject="Ежемесячный отчёт",
body="Отчёт во вложении.",
use_tls=True )

### ✅ 5. Графический интерфейс (`gui_data_loader.py`)
Все функции доступны через удобное окно:

- **Кнопки первой строки**:
  `CSV`, `Excel`, `PostgreSQL`, `REST API` — выбор источника
- **Вторая строка**:
  `Валидация`, `Очистка`, `Анализ` — обработка
- **Третья строка**:
  `Отправить отчёт` — полный пайплайн

---
### 📁 Структура проекта
. ├── main.py # Точка входа
  ├── gui_data_loader.py # GUI на Tkinter
  ├── data_loader.py # Загрузка из всех источников
  ├── data_validator.py # Проверка целостности
  ├── data_cleaner.py # Очистка данных
  ├── data_analyze.py # ML и статистика
  ├── data_report.py # Отчёты + email
  ├── requirements.txt # Зависимости
  └── README.md # Документация

## 🔐 Настройка Gmail для отправки
1. Включите двухфакторную аутентификацию
2. Создайте **App Password**: [https://myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)
3. Используйте его как пароль в форме

## 🧪 Юнит-тесты
Запуск всех тестов:
python -m unittest discover tests/

## 🛠️ Требования
- Python 3.8+
- Все зависимости из `requirements.txt`
